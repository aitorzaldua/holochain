import { CellId, CellNick, HoloHash } from '@holochain/conductor-api';
import { Player } from './player';
declare type CallZomeFunc = (zome: string, fn: string, params?: any) => Promise<any>;
declare type CellConstructorParams = {
    cellId: CellId;
    cellNick: CellNick;
    player: Player;
};
/**
 * Make zome calls from Cells.
 * `cell.call('zomeName', 'funcName', params)`
 */
export declare class Cell {
    cellId: CellId;
    cellNick: CellNick;
    _player: Player;
    constructor(o: CellConstructorParams);
    dnaHash: () => HoloHash;
    call: CallZomeFunc;
    stateDump: () => Promise<any>;
}
export {};
